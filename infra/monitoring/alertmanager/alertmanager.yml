route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs:
      - url: http://localhost:8081/alerts
        send_resolved: true
        max_alerts: 0
        http_config:
          follow_redirects: true

templates: []
